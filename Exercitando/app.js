// Lista de exervcicios chatGPT

const arrayPar = arr => arr.filter( number => number % 2 === 0)

const somaArray = arr => arr.reduce( (acc, number) => acc + number , 0)

const inverter = name => name.split().reverse().join('')
